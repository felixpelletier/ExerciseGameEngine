With spec files in creator/spec/

Mac:
	pyinstaller -y -—clean spec/guiMac.spec
	rm -rf build
	rm -rf dist/gui



Windows:
	pyinstaller -y --clean spec/guiWin.spec
	rm -rf build


*Remember to review absolute positioning for the paint calls in slview.py